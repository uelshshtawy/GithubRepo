#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
 
  int size;
  cin >> size;
  int arr[size];
  for (int i = 0; i <size; ++i) {
   cin >> arr[i];
  }
  int num;
  int temp=-1;

  cin >> num;
  for (int i = 0; i <size; ++i) {
   if(num == arr[i]){
    temp=i;
   }
   
  }
  if(temp!= -1){
    cout << temp;
   }
   else {cout << temp;
   }
    return 0;
}